# Identificação do projeto Firebase (siteeccehomo). Não é chave privada.
# A autenticação local usa Application Default Credentials:
# gcloud auth application-default login
FIREBASE_PROJECT_ID=ecce-homo-d948d
